
spring:
  profiles:
    include: oauth

  datasource:
    # RDS MariaDB
    driver-class-name: org.mariadb.jdbc.Driver
    url: jdbc:mariadb://dietblog.c8hynnw3dhnt.ap-northeast-2.rds.amazonaws.com:3306/dietblog
    username: dogyun
    password: kdk519437

  # Local MySQL
#    driver-class-name: com.mysql.cj.jdbc.Driver
#    url: jdbc:mysql://localhost:3306/knuDietBlog?serverTimezone=UTC&characterEncoding=UTF-8
#    username: dogyun
#    password: 1234

  session:
    store-type: jdbc
    jdbc:
      initialize-schema: always

  jpa:
    hibernate:
      ddl-auto: update

    generate-ddl: false
    show-sql: false
    database-platform: org.hibernate.dialect.MySQL8Dialect
    properties:
      hibernate:
        format_sql: true

  thymeleaf:
    cache: false

  servlet:
    multipart:
      enabled: true
      location:
      max-file-size: 100MB
      max-request-size: 100MB

file:
  dir: C:/Users/kdk12/Desktop/file/   # 실제 파일을 저장할 곳(local)
#  dir: /home/ec2-user/app/images/


